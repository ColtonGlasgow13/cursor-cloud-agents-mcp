# This server reads environment variables passed to its process and does not load .env files automatically.
# Put a Cursor user or service account API key here only when another tool loads this file.
CURSOR_API_KEY='<YOUR_CURSOR_API_KEY>'

# Optional overrides (defaults shown):
# CURSOR_API_BASE=https://api.cursor.com
# CURSOR_MCP_LOG_LEVEL=warn
